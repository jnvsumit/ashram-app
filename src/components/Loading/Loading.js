import React from 'react';
import { LoadingContainer, Spinner } from './Loading.styles';

const Loading = () => {
    return (
      <LoadingContainer>
        <Spinner />
      </LoadingContainer>
    );
  };
  
  export default Loading;
  